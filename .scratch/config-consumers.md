# Config Consumers Registry

Tracks the relationship between **config fields** and **runtime consumers** for Infinity-specific config that lives outside the Subspace canon.

For Subspace-canonical settings (sections like `[Bomb]`, `[Bullet]`, `[Repel]`, per-ship `Initial*` / `Maximum*` / `Upgrade*` / `*Status` / `*Energy`, etc.) the canonical view is the [`settings-pipeline.md`](settings-pipeline.md) tracker — its Subsystem and Component columns name the same `(field, consumer)` linkages this file used to duplicate. Don't add Subspace-canon rows here; add them there.

Purpose of this file (post-trim):

- **Infinity-only `ShipConfig` fields** (linear damping, turn responsiveness, restitution, radar range) — not in Subspace canon.
- **Marker components** that gate physics or rendering behavior.
- **Arena-scope config** (wall friction, prize spawner specs) — keyed off `ArenaId`, not Subspace `[Section]` keys.
- **Coordination fields** (zone-enter spawn arena, arena-local `[Spawn] X/Z`) — wire across multiple subsystems.

Rule of thumb: if it appears as a `[Section] Key` in [`REFERENCE.md`](subspace-ini-reference/REFERENCE.md), it goes in the pipeline tracker. Otherwise it goes here.

## Format

One row per `(config field, consumer)` pair. A field with three consumers gets three rows.

| Config field | Consumer | Path | Notes |
|---|---|---|---|
| `ShipConfig.linearDamping` | `PlayerDriver.update()` | `component:LinearDamping` → `RigidBody.setDamping(linear, 1.0)` | Slice S1. Per-second velocity-retention multiplier passed to mphys's native damping. Integrator applies `velocity *= pow(damping, t)` per tick. Default `0.99` (1% loss/sec at typical speed). PlayerDriver re-pokes `body.setDamping` when `applyChanges()` returns true; angularDamping=1.0 so PlayerDriver's exponential rotation ease is unambiguous angular authority. Always-on (applies during thrust too) — max-speed reach lands ~5% short. Projected at spawn by `ShipSpawnSystem`. Infinity addition (Subspace canon = no drag). |
| `ShipConfig.turnResponsiveness` | `PlayerDriver.update()` | `component:TurnResponsiveness` | Rate constant (1/sec) for angular-velocity ease-toward-target; `8.0` ≈ 95% of target in ~0.4 sec. Default `8.0` if Groovy omits it. Projected at spawn by `ShipSpawnSystem`. Infinity addition. |
| `ShipConfig.bounceRestitution` | `ContactSystem.newContact()` | `component:BounceRestitution` | Wall-bounce energy retention (`1.0` = perfectly elastic, `0` = stick). Read per ship-vs-static contact; non-ship dynamic bodies fall back to `1.0` when the component is absent. Default `1.0` if Groovy omits it. Projected at spawn by `ShipSpawnSystem`. Infinity addition. |
| `ShipConfig.radarRange` | `RadarState` (TBD, issue radar-viewport/02) | `component:RadarRange` | World-unit radius the client radar viewport displays around the ship. Drives the off-screen camera frustum size and the radar-side leaf-paging radius. Default `250.0` if Groovy omits it. Projected at spawn by `ShipSpawnSystem`. Infinity addition. |
| `ShipConfig.type` (derived) | `RadarState` (TBD, issue radar-viewport/03) | `component:RadarShapeInfo` | Server projects `RadarShapeInfo` from `cfg.type().getName() + "_blip"` (e.g. `"ship_warbird_blip"`); client-side blip-spatial registry resolves the name to a flat 2D Geometry, mirroring `SISpatialFactory.shapeName`. Color is resolved client-side from `Frequency`, NOT carried in `RadarShapeInfo`. Projected at spawn by `ShipSpawnSystem`. |
| `LargeObject` (marker) | `MPhysSystem.largeEntitySelector` | `component:LargeObject` | Wired in `GameServer.buildSystems()` as `id -> ed.getComponent(id, LargeObject.class) != null`. Partitions ECS entities between the fine `BinEntityManager` (LEAF_GRID, 32-cell) and the coarse one (TILE_GRID, 1024-cell). Set on arena ghost-cubes by `ArenaSystem` so contact-gen sees them from any fine bin within their 1024×1024 bounds. |
| `CollidesWithLargeStatics` (marker) | `PhysicsSpace.largeStaticCollisionFilter` | `component:CollidesWithLargeStatics` | Wired in `GameServer.buildSystems()` as `body -> ed.getComponent(body.id, CollidesWithLargeStatics.class) != null`. Bodies without the marker skip the entire coarse large-static contact pass before narrow phase. Set on ships at spawn by `GameEntities.createShip` (covers both player ships and AI mobs). Projectiles / sensors / non-ship dynamics deliberately stay opted out. |
| `ShapeInfo.scale` | `SISpatialFactory.createArena()` | `component:ShapeInfo` (server) → Zay-ES sync → `ModelViewState.createModel(EntityId, ShapeInfo, Mass)` reads `shapeInfo.getScale()` and forwards to `SISpatialFactory.createModel(.., double scale)` | Client wireframe cube edge = `scale / 2` (CubeFactory convention: `MBlockShape.createCube` produces an edge-length cube of `scale / 2`). Lets the client visualization track server cube changes without a hardcoded `1024`. |
| `zone.conf [ZoneEnterSpawn] Arena` | `GameSessionHostedService.GameSessionImpl.resolveInitialSpawn()` | `ini:zone.conf` → `ArenaSystem.getArenaSpawn(arenaName)` | Names the arena whose `[Spawn]` to use as the connect-time spawn point. The arena's arena-local `[Spawn] X/Z` is translated to a world coord by adding `ArenaMap.min`. Falls back to world origin if `zone.conf` is missing, the key is absent, or the named arena isn't loaded. |
| `arena.conf [Spawn] X/Z` (arena-local) | `ArenaSystem.getArenaSpawn(arenaName)` | `ini:arena.conf` via `SettingsSystem.getInt` | Arena-local coords on `[0..TILE_SIZE)`. World coord = `ArenaMap.min + (X, Z)` on `GAMEPLAY_Y`. Single source of truth for both connect-time spawn (selected via `zone.conf [ZoneEnterSpawn] Arena`) and in-arena respawn (selected via the ship's own `ArenaId`). |
| `arena.conf [Spawn] X/Z` (arena-local) | `AvatarSystem.requestShipChange()` | `component:ArenaId` → `ArenaSystem.getArenaSpawn(arenaName)` → `WarpTo(target)` | Ship-change respawn target. Reads the ship's own `ArenaId`, looks up that arena's spawn world coord, and writes a `WarpTo` for `WarpSystem` to consume. Skips the warp if the ship has no `ArenaId` (no-arena void). |
| `ArenaConfig.wallFriction` | `ContactSystem.newContact()` (body-vs-static branch) | `component:ArenaId` → `ArenaSystem.getArenaConfig(arenaName).wallFriction()` | Per-contact tangential-velocity drain. Applied directly to `body1.linearVelocity` (decompose against `contact.contactNormal`, scale tangent by `1 - wallFriction`, recombine), with `contact.friction = 0` so the resolver's friction-impulse-induced torque doesn't rotate ship heading at off-center contact points. The historical `0.0` is preserved by `ArenaConfig.EMPTY` for bodies without an `ArenaId` (projectiles, debris). DSL: `wallFriction 0.05` inside `arena { … }`. Validates `[0, 1]` at parse time. |
| `ArenaConfig.friendlyFire` | `WeaponsSystem.newContact()` (FF gate on splash + direct hit) | `component:ArenaId` (on attacker ship via `Parent`) → `ArenaSystem.getArenaConfig(arenaName).friendlyFire()` | Slice 9a. Tri-state (`0` = off, `1` = bomb splash only, `2` = all weapons) gating same-team damage. Resolved via `Parent` → attacker ship → `ArenaId` → `ArenaConfig`; falls back to `ArenaConfig.EMPTY.friendlyFire()` (= 0) when the attacker has no `ArenaId` (no-arena void, spawner-fired). Different-team and null-Frequency cases pass through. Pure-function gate extracted to package-private `WeaponsSystem.shouldDamageVictim(Integer, Integer, int, boolean)` for unit testing. DSL: `friendlyFire 1` inside `arena { … }`. Validates `[0, 2]` at parse time. Infinity-specific knob (Subspace canon uses per-weapon flags). |
| `SpawnerSpec.ttlMillis` → `Spawner.spawnedDecayMillis` | `PrizeSystem` (prize spawn path) | `component:Spawner` → `getSpawnedDecayMillis()` → projected into spawned prize's `Decay` at creation | Authored in `arena.groovy` as `ttlMs: N` inside a `spawners { spawn ... }` entry. `ArenaSystem.doLoad` materializes the spec into a `Spawner` component (field `spawnedDecayMillis`). `PrizeSystem` reads it at spawn time; `0` falls back to `CoreGameConstants.PRIZEDECAY`. Server-only — spawner entities are not synced to clients. |
| `SpawnerSpec.weightOverrides` → `PrizeWeightsOverride` | `PrizeSystem` (prize weight selection) | `component:PrizeWeightsOverride` → `getOverrides()` merged atop arena `[PrizeWeight]` defaults from `SettingsSystem` | Authored in `arena.groovy` as `weights: [Bomb: 100, ...]` inside a `spawners { spawn ... }` entry. Set as `PrizeWeightsOverride` on the spawner entity by `ArenaSystem.doLoad` only when the map is non-empty. Absent component = use arena defaults only. Server-only. |
| `SpawnerSpec.countPerPlayer` → `Spawner.countPerPlayer` | `PrizeSystem.update` (effective max-count) | `component:Spawner` → `getCountPerPlayer()` × `countPlayersInArena(spawnerArenaId)` added to `getMaxCount()` per-tick | Slice 8d (C2). Authored in `arena.groovy` as `countPerPlayer: N` inside a `spawners { spawn ... }` entry. Default `0` collapses additive scaling. Server-only — Spawner not synced. See `.claude/rules/player-scaling.md`. |
| `SpawnerSpec.radiusPerPlayer` → `Spawner.radiusPerPlayer` | `PrizeSystem.update` (effective spawn radius) | `component:Spawner` → `getRadiusPerPlayer()` × `countPlayersInArena(spawnerArenaId)` added to `SphereShape.getRadius()` per-tick | Slice 8d (C2). Authored in `arena.groovy` as `radiusPerPlayer: D` inside a `spawners { spawn ... }` entry. Default `0.0` collapses additive scaling. Server-only. |
| `SpawnerSpec.regenBatch` → `Spawner.regenBatch` | `PrizeSystem.update` (per-interval regen count) | `component:Spawner` → `getRegenBatch()` capped at `effectiveMaxCount - alive` per `spawnInterval` tick | Slice 8d (C2). Authored in `arena.groovy` as `regenBatch: N` inside a `spawners { spawn ... }` entry. Default `1` preserves pre-Slice-8d "spawn one per interval" cadence. Server-only. |
| `SpawnerSpec.hidden` → `Spawner.hidden` → spawned-prize `Hidden` | `PrizeSystem.spawnBounty` (Hidden propagation), `ModelContainer.addObject`/`updateObject` (client-side render skip) | `component:Spawner` → `isHidden()` → passed to `GameEntities.createPrize(...,hidden=true)` which stamps `Hidden` on the prize | Slice 8d (C2). Authored in `arena.groovy` as `hidden: true` inside a `spawners { spawn ... }` entry. Default `false` keeps prizes visible. `Hidden` registered in `GameServer.registerSerializers` so client sees it; `ModelContainer` skips spatial bind when present. Server still owns collision / pickup / decay — invisible prize is still pickup-able. |
| `EngineConfig.subspaceVelocityScale` | `WeaponsSystem.getAttackInfo()` (cases GUN / BOMB / BURST) | `EngineConfigSystem.get()` → `EngineConfig.subspaceVelocityScale()` → `effectiveProjectileSpeed(rawSubspaceValue, scale, cap)` | Slice 10. Game-wide multiplier translating per-ship Subspace velocity values (`GunSpeed` / `BombSpeed` / `BurstSpeed` components, raw int) into jME world units / sec at fire time. Default `0.01` produces SVS canon `2000 → 20` jME and trench warbird's `5000 → 50`. Lives in `infinity/src/main/resources/engine.groovy` (developer-tunable, packaged in jar — distinct from operator-tunable per-arena/zone configs). Loaded once at server startup by `EngineConfigSystem`. Infinity addition (engine-tier — fourth scope above preset/arena/zone). |
| `EngineConfig.bombThrustScale` | `WeaponsSystem.applyBombRecoil()` (BOMB + GRAVBOMB fire) | `EngineConfigSystem.get()` → `EngineConfig.bombThrustScale()` → `recoilImpulse(rawThrust, scale, cap, shipOrientation)` → `effectiveProjectileSpeed` | Slice S2 + S2-cal. Engine-tier scale dedicated to bomb recoil — distinct from `subspaceVelocityScale` (projectile speeds) per S2-cal playtest. Default `0.0005` (playtest-tuned) maps SVS canon `BombThrust 400 → 0.2 jME/sec` backward impulse — a subtle nudge (~1% of ship max-speed). |
| `EngineConfig.shipMaxSpeedScale` | `PlayerDriver.update()` | `EngineConfigSystem.get()` → `EngineConfig.shipMaxSpeedScale()` → `speed.getSpeed() × scale` for the maxSpeed cap | Slice S1-cal. Engine-tier scale dedicated to ship max-speed cap. Multiplies the ship's raw `Speed` (Subspace velocity units) at consumer time so ships.groovy can author Subspace-canonical values (`speed max: 6000`) while landing in the jME world-units range. Default `0.01` (playtest-tuned) maps trench warbird's `Speed 2000 → 20 jME/sec` cap (steady-state ~19.75 under `LinearDamping 0.99`), ~40% of bullet velocity. PlayerDriver receives `EngineConfigSystem` via the `MovementInputSystem.PlayerContainer.addObject` constructor; falls back to `EngineConfig.DEFAULTS` when null (test harnesses). |
| `EngineConfig.maxProjectileSpeedJme` | `WeaponsSystem.getAttackInfo()` (cases GUN / BOMB / BURST) | `EngineConfigSystem.get()` → `EngineConfig.maxProjectileSpeedJme()` → absolute-value clamp inside `effectiveProjectileSpeed` | Slice 10. Game-wide post-translation cap (jME world units / sec). Bounds the absolute projectile speed so legacy SVS values (e.g. trench javelin's `BulletSpeed 64636` → `646.36` raw) don't break physics. Negative inputs preserve sign and clamp to `-cap` (slice 10b backward firing). Default `100.0`. Lives in `engine.groovy`. Infinity addition. |
| `EngineConfig.bulletRadius` | `WeaponsSystem.createProjectile*` (Bullet path) → `GameEntities.createBullet()` `ShapeInfo.create` arg; also `WeaponsLogic.applyProjectileRadiusOffset` for spawn-point offset | engine-tier — read once at server init from `engine.groovy`; passed as `double radius` arg into the api-side factory | Slice projectile-radius-pattern4. Engine-tier collision radius (Moss/Infinity ShapeInfo, jME world units; 1 unit ≈ 1 tile). Default `0.125` matches retired `CorePhysicsConstants.BULLETSIZERADIUS`. Subspace canon does not author per-projectile collision radius. Engine-tier (developer-tuned, identical across arenas). |
| `EngineConfig.bombRadius` | `WeaponsSystem.createProjectile*` (Bomb path) → `GameEntities.createBomb()` / `GameEntities.createDelayedBomb()`; also `WeaponsLogic.applyProjectileRadiusOffset` for spawn-point offset | engine-tier — read once at server init from `engine.groovy`; passed as `double radius` arg into the api-side factory | Slice projectile-radius-pattern4. Engine-tier collision radius (Moss/Infinity ShapeInfo). Default `0.5` matches retired `BOMBSIZERADIUS`. |
| `EngineConfig.mineRadius` | `WeaponsSystem` (mine path) → `GameEntities.createMine()` | engine-tier — read once at server init from `engine.groovy`; passed as `double radius` arg into the api-side factory | Slice projectile-radius-pattern4. Engine-tier collision radius (Moss/Infinity ShapeInfo). Default `0.5` matches retired `MINESIZERADIUS`. |
| `EngineConfig.thorRadius` | `ConsumableSystem` (thor handler) → `GameEntities.createThor()`; also `ConsumableSystem.getActionPosition` (spawn-point offset) | engine-tier — read once at server init from `engine.groovy`; passed as `double radius` arg into the api-side factory | Slice projectile-radius-pattern4. Engine-tier collision radius (Moss/Infinity ShapeInfo). Default `0.5` matches retired `THORSIZERADIUS`. |
| `EngineConfig.prizeRadius` | `PrizeSystem.spawnBounty` + `PrizeSystem.spawnDeathPrize` → `GameEntities.createPrize()` | engine-tier — read once at server init from `engine.groovy`; passed as `double radius` arg into the api-side factory | Slice projectile-radius-pattern4. Engine-tier collision radius (Moss/Infinity ShapeInfo). Default `0.5` matches retired `PRIZESIZERADIUS`. |
| `EngineConfig.burstRadius` | `WeaponsSystem` (burst path) → `GameEntities.createBurst()` | engine-tier — read once at server init from `engine.groovy`; passed as `double radius` arg into the api-side factory | Slice projectile-radius-pattern4. Engine-tier collision radius (Moss/Infinity ShapeInfo). Default `0.125` matches retired `BURSTSIZERADIUS`. |
| `EngineConfig.repelRadius` | `ConsumableSystem` (repel handler) → `GameEntities.createRepel()` | engine-tier — read once at server init from `engine.groovy`; passed as `double radius` arg into the api-side factory | Slice projectile-radius-pattern4. Engine-tier collision radius (Moss/Infinity ShapeInfo). Default `0.125` matches retired `REPELRADIUS`. |
| `EngineConfig.over1Radius` | `MapSystem.spawnTileEntity` → `GameEntities.createAsteroidSmall()` | engine-tier — read once at server init from `engine.groovy`; passed as `double radius` arg into the api-side factory | Slice projectile-radius-pattern4. Engine-tier collision radius (Moss/Infinity ShapeInfo). Default `0.5` matches retired `OVER1SIZERADIUS`. |
| `EngineConfig.over2Radius` | `MapSystem.spawnTileEntity` → `GameEntities.createAsteroidMedium()` | engine-tier — read once at server init from `engine.groovy`; passed as `double radius` arg into the api-side factory | Slice projectile-radius-pattern4. Engine-tier collision radius (Moss/Infinity ShapeInfo). Default `1.0` matches retired `OVER2SIZERADIUS`. |
| `EngineConfig.over5Radius` | `MapSystem.spawnTileEntity` → `GameEntities.createOver5()` | engine-tier — read once at server init from `engine.groovy`; passed as `double radius` arg into the api-side factory | Slice projectile-radius-pattern4. Engine-tier collision radius (Moss/Infinity ShapeInfo). Default `0.1` matches retired `OVER5SIZERADIUS`. |
| `EngineConfig.flagRadius` | `MapSystem.spawnTileEntity` → `GameEntities.createTurfStationaryFlag()` | engine-tier — read once at server init from `engine.groovy`; passed as `double radius` arg into the api-side factory | Slice projectile-radius-pattern4. Engine-tier collision radius (Moss/Infinity ShapeInfo). Default `0.5` matches retired `FLAGSIZERADIUS`. |
| `EngineConfig.shipRadius` | `GameSessionHostedService.createPlayerShip` → `GameEntities.createPlayerShip(... double radius)` → `ShapeNames.createShip(byte, ed, double)` → `ShapeInfo.create`; AND `AvatarSystem.requestShipChange` (`ShipChange` consumer; reads `engineConfigSystem.get().shipRadius()` directly) | engine-tier — read once at server init from `engine.groovy`; passed as `double radius` arg into the api-side factory (or read inline at the consumer for the in-flight `ShipChange` path) | Slice S6. Engine-tier collision radius (Moss/Infinity ShapeInfo, jME world units). Default `1.0` matches retired `CorePhysicsConstants.SHIPSIZERADIUS`. Subspace canon authors per-ship `Radius` (REFERENCE.md, default 14 px); Infinity divergence — engine-tier global, not per-ship — collision radius is identical across all ships in the build. |

**Column meaning:**

- **Config field** — the canonical typed path, e.g. `ShipConfig.linearDamping`, `ArenaConfig.wallFriction`. Match the Groovy DSL name.
- **Consumer** — `ClassName.methodName()` or `ClassName (field)`. Include the method when useful for finding the read site.
- **Path** — how the consumer obtains the value. Typical entries:
  - `template` — direct read from `ConfigRegistry.get(type).field()`
  - `component:ComponentName` — read from a per-entity Zay-ES component derived from the template
  - `setting` — string-keyed `settings.getInt(arena, section, key, default)` (legacy; tag for migration)
- **Notes** — anything special: upgrade semantics, caching, tick frequency, etc.

## When to append

- Adding a new field to an Infinity-specific `*Config` record (not Subspace canon) → add rows for each planned consumer (or a row with consumer = `TBD` if wiring is staged).
- Changing a consumer to read a new field → add a row.
- Removing a consumer → delete its row; if field has zero consumers after, flag it as orphan config for review.
- Adding a new consumer class reading existing config → add one row per field it reads.

Don't log per-tick / per-call reads inside one method — one row per `(field, consumer class)` pair is enough granularity.

## What NOT to track here

- **Subspace-canonical settings** — those go in [`settings-pipeline.md`](settings-pipeline.md). Adding them here duplicates the pipeline tracker and creates drift risk.
- Per-entity component values that don't derive from config (e.g. current energy, current velocity).
- Settings nothing reads yet — they'd all appear as "orphan config". Don't preload; only add rows when a consumer is wired.
