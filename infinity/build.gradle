plugins {
    id 'infinity.app-with-assets'
    id 'com.github.ben-manes.versions' version '0.51.0'
}

mainClassName = "infinity.Main"

// version is inherited from the root build.gradle subprojects block

// Redirect JVM fatal-error logs (hs_err_pid*.log) to build/crash-logs/ so they
// don't clutter the repo root. Swept by `./gradlew clean`.
def crashLogDir = layout.buildDirectory.dir("crash-logs").get().asFile
crashLogDir.mkdirs()
applicationDefaultJvmArgs = [
    "-Xmx1024m", "-Xms512m", "-XX:MaxDirectMemorySize=1024m",
    "--add-opens=java.base/jdk.internal.ref=ALL-UNNAMED",
    "-XX:ErrorFile=${crashLogDir}/hs_err_pid%p.log",
    // Route java.util.logging → log4j2 (paired with the log4j-jul runtime dep
    // above). Must be set before any j.u.l class is touched, hence here in
    // the JVM args rather than at runtime.
    "-Djava.util.logging.manager=org.apache.logging.log4j.jul.LogManager"
]

// Force X11 mode for LWJGL3 on Wayland systems (fixes EGL window creation issues)
tasks.register('runX11', JavaExec) {
    group = 'application'
    description = 'Run the game with X11 backend (Wayland fix)'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = mainClassName
    jvmArgs = applicationDefaultJvmArgs
    environment 'WAYLAND_DISPLAY', ''
}

// macOS requires GLFW to live on the JVM's first/main thread; without
// -XstartOnFirstThread the LWJGL window creation aborts. Pair with
// java.awt.headless=true so the BMP-decode path (BitMap extends JPanel,
// createImage via MemoryImageSource) doesn't try to spin up AWT/AppKit
// peers — that deadlocks against GLFW since both want the main thread.
tasks.register('runMac', JavaExec) {
    group = 'application'
    description = 'Run the game on macOS (-XstartOnFirstThread, headless AWT)'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = mainClassName
    jvmArgs = applicationDefaultJvmArgs + ['-XstartOnFirstThread', '-Djava.awt.headless=true']
}

// Map inspection tool: ./gradlew :infinity:analyzeMaps --args "trench.lvl trench2.lvl"
tasks.register('analyzeMaps', JavaExec) {
    group = 'diagnostics'
    description = 'Analyze one or two .lvl maps (counts, categories, position diff)'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'infinity.tools.MapAnalyzer'
    standardOutput = System.out
    errorOutput = System.err
}

// Batch tile-ID survey: ./gradlew :infinity:surveyMaps            (scans assets/Maps/)
//                       ./gradlew :infinity:surveyMaps --args "some.lvl another.lvl"
tasks.register('surveyMaps', JavaExec) {
    group = 'diagnostics'
    description = 'Batch tile-ID survey across .lvl files (histogram + gap analysis)'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'infinity.tools.MapTileSurvey'
    standardOutput = System.out
    errorOutput = System.err
}

// GameServer writes a persistent block-world cache to infinity/world.db/ (relative
// to the JVM working dir). It lives outside build/ because it's runtime state, not
// a build artifact — so `clean` doesn't sweep it by default. Wipe it here so
// `./gradlew clean` gives a fully fresh world on the next run.
clean {
    delete 'world.db'
}

dependencies {
    // logging
    implementation "org.slf4j:slf4j-api:$slf4jVersion"
    implementation "org.apache.logging.log4j:log4j-api:$log4jVersion"
    implementation "org.apache.logging.log4j:log4j-core:$log4jVersion"
    runtimeOnly "org.apache.logging.log4j:log4j-slf4j-impl:$log4jVersion"
    // Bridge java.util.logging → log4j2 so JME's render-thread errors and
    // any other JUL output land in infinity.log instead of disappearing
    // into stderr (which itch.io's launcher discards). Activated by the
    // -Djava.util.logging.manager=... JVM arg below.
    runtimeOnly "org.apache.logging.log4j:log4j-jul:$log4jVersion"

    //https://mvnrepository.com/artifact/com.google.code.findbugs/jsr305
    implementation "com.google.code.findbugs:jsr305:3.0.2"

    // Ini4j to parse settings files (legacy arena.conf path; phasing out per conf-fragments-to-groovy PRD)
    implementation "org.ini4j:ini4j:0.5.4"

    // For Lemur styles
    implementation "org.codehaus.groovy:groovy-all:${groovyVersion}"

    // jMonkeyEngines
    implementation "org.jmonkeyengine:jme3-core:$jmeVersion"
    implementation "org.jmonkeyengine:jme3-desktop:$jmeVersion"
    implementation "org.jmonkeyengine:jme3-effects:$jmeVersion"
    implementation "org.jmonkeyengine:jme3-lwjgl3:$jmeVersion"
    implementation "org.jmonkeyengine:jme3-plugins:$jmeVersion"

    // Simsilica
    implementation "com.simsilica:lemur:$lemurVersion"
    implementation "com.simsilica:lemur-proto:$lemurProtoVersion"
    implementation "com.simsilica:lemur-props:$lemurPropsVersion"
    implementation "com.simsilica:pager:$pagerVersion"
    implementation "com.simsilica:sim-ethereal:$simEtherealVersion"
    implementation "com.simsilica:sim-fx:$simFxVersion"
    implementation "com.simsilica:sim-math:$simMathVersion"
    implementation "com.simsilica:sio2:$sio2Version"
    implementation "com.simsilica:zay-es:$zayEsVersion"
    implementation "com.simsilica:zay-es-net:$zayEsNetVersion"

    // Simsilica MOSS
    implementation "com.simsilica:mblock:$mossVersion"
    implementation "com.simsilica:mblock-physb:$mossVersion"
    implementation "com.simsilica:mworld:$mossVersion"
    implementation "com.simsilica:sio2-mblock:$mossVersion"
    implementation "com.simsilica:sio2-mphys:$mossVersion"
    implementation "com.simsilica:bpos:$mossVersion"
    implementation "com.simsilica:crig:$mossVersion"

    // Local dependencies
    implementation project(":api")
    runtimeOnly project(":modules")

    // Test-only — architecture boundary enforcement
    testImplementation "com.tngtech.archunit:archunit-junit4:1.4.2"
    // Put modules on the test compile classpath so ArchUnit can analyse them
    testImplementation project(":modules")
}

//checkstyle {
//    sourceSets = []
//}

//jar {
//    archiveBaseName = "subspace-infinity"
//}


distributions {
    main {
        distributionBaseName = "infinity"
    }
}

// =============================================================================
// jpackage - Native application bundling (replacement for parcl)
// =============================================================================
// Requires JDK 14+ to run jpackage. Creates native installers with bundled JRE.
//
// Usage:
//   ./gradlew jpackage          - Creates native package for current OS
//   ./gradlew jpackageImage     - Creates app image (no installer)
//
// The tasks detect your OS and create the appropriate format:
//   - Windows: .exe installer (or .msi with WiX toolset)
//   - macOS: .dmg or .pkg
//   - Linux: .deb or .rpm
// =============================================================================

def jpackageOutputDir = layout.buildDirectory.dir("jpackage")
def jpackageImageDir = layout.buildDirectory.dir("jpackage-image")

tasks.register('jpackageImage', Exec) {
    group = 'distribution'
    description = 'Creates a native application image using jpackage (no installer)'
    dependsOn installDist
    
    def installDir = tasks.installDist.destinationDir
    def os = org.gradle.internal.os.OperatingSystem.current()
    
    doFirst {
        jpackageImageDir.get().asFile.mkdirs()
    }
    
    executable = 'jpackage'
    args = [
        '--type', 'app-image',
        '--name', 'Subspace-Infinity',
        '--app-version', version,
        '--vendor', 'Asser Fahrenholz',
        '--copyright', 'Copyright 2024 Asser Fahrenholz',
        '--description', 'A Subspace/Continuum-inspired space shooter',
        '--input', "${installDir}/lib",
        '--main-jar', "infinity-${version}.jar",
        '--main-class', mainClassName,
        '--dest', jpackageImageDir.get().asFile.absolutePath,
        '--java-options', '-Xmx1024m',
        '--java-options', '-Xms512m',
        '--java-options', '-XX:MaxDirectMemorySize=1024m',
        '--java-options', '--add-opens=java.base/jdk.internal.ref=ALL-UNNAMED',
        // Route JME's java.util.logging output through log4j so render-thread
        // exceptions land in infinity.log on packaged builds (itch.io's
        // launcher discards stderr otherwise).
        '--java-options', '-Djava.util.logging.manager=org.apache.logging.log4j.jul.LogManager'
    ]
    
    // Add icon if it exists
    def iconFile = os.isWindows() ? file('src/main/resources/icon.ico') :
                   os.isMacOsX() ? file('src/main/resources/icon.icns') :
                   file('src/main/resources/icon.png')
    if (iconFile.exists()) {
        args += ['--icon', iconFile.absolutePath]
    }
}

tasks.register('jpackage', Exec) {
    group = 'distribution'
    description = 'Creates a native installer using jpackage'
    dependsOn installDist
    
    def installDir = tasks.installDist.destinationDir
    def os = org.gradle.internal.os.OperatingSystem.current()
    
    doFirst {
        jpackageOutputDir.get().asFile.mkdirs()
    }
    
    executable = 'jpackage'
    args = [
        '--name', 'Subspace-Infinity',
        '--app-version', version,
        '--vendor', 'Asser Fahrenholz',
        '--copyright', 'Copyright 2024 Asser Fahrenholz',
        '--description', 'A Subspace/Continuum-inspired space shooter',
        '--input', "${installDir}/lib",
        '--main-jar', "infinity-${version}.jar",
        '--main-class', mainClassName,
        '--dest', jpackageOutputDir.get().asFile.absolutePath,
        '--java-options', '-Xmx1024m',
        '--java-options', '-Xms512m',
        '--java-options', '-XX:MaxDirectMemorySize=1024m',
        '--java-options', '--add-opens=java.base/jdk.internal.ref=ALL-UNNAMED',
        // Route JME's java.util.logging output through log4j so render-thread
        // exceptions land in infinity.log on packaged builds.
        '--java-options', '-Djava.util.logging.manager=org.apache.logging.log4j.jul.LogManager'
    ]
    
    // OS-specific installer type
    if (os.isWindows()) {
        args += ['--type', 'exe', '--win-shortcut', '--win-menu', '--win-dir-chooser']
    } else if (os.isMacOsX()) {
        args += ['--type', 'dmg']
    } else {
        args += ['--type', 'deb']  // or 'rpm' for Red Hat-based
    }
    
    // Add icon if it exists
    def iconFile = os.isWindows() ? file('src/main/resources/icon.ico') :
                   os.isMacOsX() ? file('src/main/resources/icon.icns') :
                   file('src/main/resources/icon.png')
    if (iconFile.exists()) {
        args += ['--icon', iconFile.absolutePath]
    }
}