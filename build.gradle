subprojects {

    version='1.0.4-SNAPSHOT'

    //For now we want the latest version of everything except JMonkeyEngine
    //Could also specify major versions like 4.+
    ext.jmeVersion = '3.9.0-stable'
    ext.lemurVersion = '+'
    ext.lemurProtoVersion = '+'
    ext.lemurPropsVersion = '+'
    ext.simMathVersion = '+'
    ext.sio2Version = '+'
    ext.simEtherealVersion = '+'
    ext.zayEsVersion = '+'
    ext.zayEsNetVersion = '+'

    ext.groovyVersion = '+'
    ext.gsonVersion = '2.11.0'  // pinned to stable release
    ext.log4jVersion = '2.24.3'  // pinned to stable release
    ext.slf4jVersion = '2.0.16'  // pinned to stable release
    ext.guavaVersion = '+'

    ext.mossVersion = "+"

    ext.pagerVersion = '1.0.1-SNAPSHOT'
    ext.simFxVersion = '1.0.1-SNAPSHOT'

    ext.jetbrainsAnnotationsVersion = '+'
    ext.junitVersion = '+'
}